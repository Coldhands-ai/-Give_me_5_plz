<Window x:Class="PayrollSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Вход в систему" 
        Height="550" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}" SizeToContent="WidthAndHeight">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="24*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <materialDesign:Card Margin="40,40,22,40" Padding="30">
            <StackPanel>
                <TextBlock Text="Радость бухгалтера" 
                           FontSize="24" 
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Foreground="{DynamicResource PrimaryHueDarkBrush}"
                           Margin="0,0,0,10"/>

                <TextBlock Text="Система начисления заработной платы" 
                           FontSize="12" 
                           HorizontalAlignment="Center"
                           Foreground="Gray"
                           Margin="0,0,0,40"/>

                <TextBox x:Name="txtUsername"
                         materialDesign:HintAssist.Hint="Имя пользователя"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         FontSize="14"
                         Margin="0,0,0,20"/>

                <PasswordBox x:Name="txtPassword"
                             materialDesign:HintAssist.Hint="Пароль"
                             Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                             FontSize="14"
                             Margin="0,0,0,30"/>

                <Button Content="ВОЙТИ" 
                        Click="BtnLogin_Click"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Height="45"
                        FontSize="14"/>

                <TextBlock x:Name="txtError" 
                           Foreground="Red" 
                           TextAlignment="Center"
                           Margin="0,20,0,0"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>
