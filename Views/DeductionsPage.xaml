<Page x:Class="PayrollSystem.Views.DeductionsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Удержания"
      Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Справочник удержаний" 
                   FontSize="24" 
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox x:Name="txtDeductionName"
                     materialDesign:HintAssist.Hint="Название удержания"
                     Width="300"
                     Margin="0,0,15,0"/>
            <Button Content="Добавить" 
                    Click="BtnAdd_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="0,0,10,0"/>
            <Button Content="Удалить выбранное" 
                    Click="BtnDelete_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Foreground="Red"
                    BorderBrush="Red"/>
        </StackPanel>

        <DataGrid Grid.Row="2" 
                  x:Name="dgDeductions"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  materialDesign:DataGridAssist.CellPadding="13 8 8 8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding DeductionName}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
